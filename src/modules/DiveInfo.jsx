import styles from '../styles/DiveInfo.module.css';

const DiveInfo = () => {
  return (
    <div className={`${styles.diveInfoContainer}`}>
      <div className={`${styles.title}`}>Explore the Depths</div>
    </div>
  );
};

export default DiveInfo;
