import styles from '../styles/Nav.module.css';

const Nav = () => {
  return (
    <div className={`${styles.navContainer}`}>
      <div className={`${styles.title}`}>Paseo Del Mar</div>
    </div>
  );
};

export default Nav;
